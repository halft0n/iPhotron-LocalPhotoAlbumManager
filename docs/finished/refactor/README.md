# Finished Refactor Archives

This directory contains completed refactor records. These documents are useful
for migration history and verification context, but they are not the current
implementation entry point.

Current architecture source of truth:

- `docs/architecture.md`
- `AGENT.md`
- focused guardrails under `docs/misc/`

Archives:

- `vnext-2026-06/`: completed RuntimeContext/LibrarySession vNext cleanup and
  large-library performance migration records.

The older `docs/finished/referactor/` directory is intentionally left in place
with its historical spelling.
